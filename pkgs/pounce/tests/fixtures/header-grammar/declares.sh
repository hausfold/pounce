#!/bin/bash
# The three declarations (CommandRisk): all parsed, `confirm` FIRST-wins like
# `submenu`, and `1` spelled as a digit still means true.
# pounce: name = Declares
# pounce: mutates = true
# pounce: confirm = false
# pounce: confirm = true
# pounce: network = 1
